import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/recipes-identity";

describe("@sderickson/recipes-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
