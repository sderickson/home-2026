notesFilesUploadRecipes:
  summary: "Upload a new file for a recipe note (multiple files allowed). Admin only."
  operationId: "notesFilesUploadRecipes"

  parameters:
    - name: "id"
      in: "path"
      required: true
      schema:
        type: string
      description: "Recipe id"
    - name: "noteId"
      in: "path"
      required: true
      schema:
        type: string
      description: "Recipe note id"

  requestBody:
    required: true
    content:
      multipart/form-data:
        schema:
          type: object
          required:
            - file
          properties:
            file:
              type: string
              format: binary
              description: "The file to upload (stored in Azure)."

  responses:
    "200":
      description: "Created recipe note file metadata."
      content:
        application/json:
          schema:
            $ref: "../../schemas/recipe-note-file-info.yaml"

    "401":
      description: "Unauthorized - missing or invalid auth headers, or not logged in."
      content:
        application/json:
          schema:
            $ref: "../../schemas/error.yaml"

    "403":
      description: "Forbidden - user does not have required privileges (admin only)."
      content:
        application/json:
          schema:
            $ref: "../../schemas/error.yaml"

    "404":
      description: "Not Found"
      content:
        application/json:
          schema:
            $ref: "../../schemas/error.yaml"
