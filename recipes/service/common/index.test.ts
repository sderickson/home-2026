import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/recipes-service-common";

describe("@sderickson/recipes-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
