{
  "type": "object",
  "properties": {
    "ADMIN_EMAILS": {
      "type": "string",
      "description": "Comma-separated list of admin emails"
    },
    "RECIPES_SERVICE_HTTP_HOST": {
      "type": "string"
    }
  },
  "required": ["RECIPES_SERVICE_HTTP_HOST", "ADMIN_EMAILS"]
}
