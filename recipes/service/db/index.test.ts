import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/recipes-db";

describe("@sderickson/recipes-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
