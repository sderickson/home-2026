import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/recipes-http";

describe("@sderickson/recipes-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
