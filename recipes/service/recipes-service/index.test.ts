import { expect, describe, it } from "vitest";
import * as mainExports from "@sderickson/recipes-service";

describe("@sderickson/recipes-service", () => {
  it("should be defined", () => {
    expect(mainExports).toBeDefined();
  });
});
