{
  "type": "object",
  "properties": {
    "UNSPLASH_API_KEY": {
      "type": "string",
      "description": "Unsplash API Access Key. Create an application at https://unsplash.com/developers to obtain an access key. Required for live API calls; omit or set to 'mock' to use the mock client (e.g. in tests or when the integration is disabled)."
    }
  },
  "required": []
}
