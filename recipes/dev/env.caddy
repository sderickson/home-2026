# Caddy env for recipes/dev — DOMAIN is the CORS Origin RE2 allowlist (not the DNS name).
# $$ → single $ after compose/env interpolation.
DOMAIN=^http://((app|admin|account)\.recipes|recipes|app|auth|admin|account)\.docker\.localhost$$|^http://docker\.localhost$$
PROTOCOL=http
CSP_SCRIPT_SRC_EXTRA=http://kratos.docker.localhost
CSP_REPORT_URI=http://api.recipes.docker.localhost/csp-violations
