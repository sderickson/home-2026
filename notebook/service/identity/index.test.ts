import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/notebook-identity";

describe("@sderickson/notebook-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
