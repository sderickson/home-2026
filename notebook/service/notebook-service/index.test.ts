import { expect, describe, it } from "vitest";
import * as mainExports from "@sderickson/notebook-service";

describe("@sderickson/notebook-service", () => {
  it("should be defined", () => {
    expect(mainExports).toBeDefined();
  });
});
