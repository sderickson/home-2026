import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/notebook-service-common";

describe("@sderickson/notebook-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
