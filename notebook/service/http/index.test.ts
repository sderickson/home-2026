import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/notebook-http";

describe("@sderickson/notebook-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
