import { expect, describe, it } from "vitest";
import * as mainExports from "@sderickson/hub-service";

describe("@sderickson/hub-service", () => {
  it("should be defined", () => {
    expect(mainExports).toBeDefined();
  });
});
