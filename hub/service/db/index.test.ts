import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/hub-db";

describe("@sderickson/hub-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
