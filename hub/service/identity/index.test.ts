import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/hub-identity";

describe("@sderickson/hub-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
