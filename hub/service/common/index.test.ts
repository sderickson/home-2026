import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/hub-service-common";

describe("@sderickson/hub-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
