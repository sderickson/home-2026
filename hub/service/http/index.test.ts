import { describe, it, expect } from "vitest";
import * as exports from "@sderickson/hub-http";

describe("@sderickson/hub-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
