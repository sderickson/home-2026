# Copy to .env.kratos.secrets and fill in with high-entropy (>= 256 bit) values.
# These override the secrets: block in kratos.yml at runtime.
SECRETS_COOKIE=
SECRETS_CIPHER=
KRATOS_COURIER_HTTP_HOST=monolith:3000
