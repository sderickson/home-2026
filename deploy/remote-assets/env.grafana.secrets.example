# Copy to `.env.grafana.secrets` next to docker-compose (gitignored) and set a strong password.
# Grafana OSS reads these on startup for the built-in admin user.
# @see https://grafana.com/docs/grafana/latest/setup-grafana/configure-docker/

GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=
