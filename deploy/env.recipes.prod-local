# Used by Dockerfile.prod to build recipes client static assets (hub domain model)
DOMAIN=docker.localhost
CLIENT_SUBDOMAINS=recipes,app.recipes,admin.recipes,account.recipes
SERVICE_SUBDOMAINS=identity,api.recipes
DEPLOYMENT_NAME=prodlocal
PROTOCOL=http
TZ=UTC
