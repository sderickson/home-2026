# Used by Dockerfile.prod to build notebook client static assets (hub domain model)
DOMAIN=docker.localhost
CLIENT_SUBDOMAINS=notebook,app.notebook,admin.notebook,account.notebook
SERVICE_SUBDOMAINS=identity,api.notebook
DEPLOYMENT_NAME=prodlocal
PROTOCOL=http
TZ=UTC
