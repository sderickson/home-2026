# Used by Dockerfile.prod to build hub client static assets (hub domain model)
DOMAIN=docker.localhost
CLIENT_SUBDOMAINS=,app,auth,account,admin
SERVICE_SUBDOMAINS=identity,hub
DEPLOYMENT_NAME=prodlocal
PROTOCOL=http
TZ=UTC
